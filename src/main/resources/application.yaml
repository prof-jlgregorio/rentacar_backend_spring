server:
  port: 8081
  servlet:
    context-path: /rentacar/api
spring:
  application:
    name: RentACar
  datasource:
    url: jdbc:postgresql://localhost:5432/rentacar
    username: postgres
    password: postdba
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
